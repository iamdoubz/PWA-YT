node_modules/
dist/
dev-dist/

# Real secrets live here (PWA_YT_COOKIE_KEY, SMTP password). .env.example is
# the tracked template; .env itself must never be committed.
.env

# Generated test fixtures — regenerate with `npm run fixtures`.
# 86 MB of sine wave does not belong in git.
app/public/media/

# Server: the database is state, not source. Rebuilt by db.init().
server/pwa-yt.db
server/pwa-yt.db-wal
server/pwa-yt.db-shm
server/.venv/
__pycache__/
server/scratch/
